#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../repl.history/bin/repl.history" "$@"
  ret=$?
else 
  node  "$basedir/../repl.history/bin/repl.history" "$@"
  ret=$?
fi
exit $ret
